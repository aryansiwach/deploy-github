PORT=3000
MONGO_URI=mongodb://127.0.0.1:27017/authentication
CLIENT_URL=http://localhost:5173

# Generate a strong random value for this -- never reuse a value that has
# ever been committed to git.
KEY=

# Gmail address + an App Password (myaccount.google.com/apppasswords),
# used to send password-reset emails. Never a real account password.
EMAIL_USER=
EMAIL_PASS=
